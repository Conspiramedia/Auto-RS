// ============================================================
// RS AUTO — Каркас разделов приложения.
// ============================================================
// Нижней панели вкладок здесь больше НЕТ. Раньше в ней было пять
// пунктов (Каталог / Избранное / + / Сообщения / Профиль), но на сайте
// такой панели не существует: вся навигация открывается из меню в
// шапке. Приложение повторяет сайт, поэтому разделы переехали в бургер
// (AppSearchHeader), а размещение объявления — в зелёный CTA «Продать
// авто» там же.
//
// Каркас остался, потому что его требует ShellRoute в go_router: он
// держит общий фон и SafeArea для всех разделов сразу. Бейджи
// непрочитанных переехали на иконку меню в шапке.
// ============================================================

import 'package:flutter/material.dart';

import '../../core/theme/app_brand.dart';

class HomeShell extends StatelessWidget {
  const HomeShell({super.key, required this.child});

  /// Текущий экран раздела (подставляет go_router).
  final Widget child;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppBrandColors.bg,
      body: child,
    );
  }
}
