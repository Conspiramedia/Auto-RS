// ============================================================
// AUTO.RS — Глобальные константы приложения.
// ВНИМАНИЕ: комиссия платформы здесь — только для ОТОБРАЖЕНИЯ
// разбивки цены на клиенте. Реальный расчёт итоговой суммы
// выполняет триггер calc_booking_totals на сервере (Thick Backend).
// ============================================================

class AppConstants {
  AppConstants._();

  // Комиссия платформы (10%) — синхронизирована с сервером.
  // Используется ТОЛЬКО для превью разбивки цены в UI до создания брони.
  static const double platformCommissionRate = 0.10;

  // Валюта по умолчанию (в БД храним EUR)
  static const String defaultCurrency = 'EUR';

  // Ориентировочный курс EUR → RSD для конвейера отображения.
  // В проде заменить на актуальный курс из справочника/внешнего API.
  static const double eurToRsdRate = 117.0;

  // Лимиты
  static const int maxCarImages = 15;    // максимум фото в объявлении
  static const int minRentDays = 1;      // минимальный срок аренды, суток
}
