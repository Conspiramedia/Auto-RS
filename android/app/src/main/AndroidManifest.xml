<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Push-уведомления. С Android 13 (API 33) разрешение спрашивается у
         пользователя в рантайме; на более старых версиях оно выдаётся при
         установке. Без объявления здесь запрос из приложения не сработает. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="RS Auto"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Тап по push-уведомлению, когда приложение выгружено.
                 Без этого фильтра FCM не доставит данные уведомления
                 в getInitialMessage() при холодном старте. -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

            <!-- App Links: ссылка на объявление с сайта открывается в
                 приложении, а не в браузере.
                 Путь /car/* совпадает с роутом приложения (см. app_router).

                 autoVerify="true" заставляет Android при установке проверить
                 https://rsauto-rs.vercel.app/.well-known/assetlinks.json.
                 ВАЖНО: на *.vercel.app проверка НЕ пройдёт — субдомены
                 публичного хостинга входят в Public Suffix List, и Android
                 не связывает их с приложением. До перехода на собственный
                 домен ссылки будут открываться через диалог выбора
                 приложения, а не автоматически. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="rsauto-rs.vercel.app"
                    android:pathPrefix="/car/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
